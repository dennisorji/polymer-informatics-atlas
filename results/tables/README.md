# Result tables

This directory contains lightweight, release-facing summary tables reproduced from the executed notebook workflow. Values are reported at the precision preserved by the final notebook outputs; detailed fold-level, per-polymer, and diagnostic tables remain in the notebooks.

The tables are descriptive outputs of this project and should be interpreted with the claim boundaries documented in the root README and `REPRODUCIBILITY.md`.
